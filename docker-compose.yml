services:
  websocket-server:
    build: .
    container_name: websocket-test-server
    ports:
      - "3335:3335"
    environment:
      - NODE_ENV=production
      - PORT=3335
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:3335/"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
